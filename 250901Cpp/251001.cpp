#include "MyIncludes.hpp"

//--------------------
int main()
//--------------------
{
	int nums[1000];

	for each(int& num in nums)
	{
		num = uid(dre);
	}

	for each(int& num in nums)
	{
		print("{:10}", num);
	}

}